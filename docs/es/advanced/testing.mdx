---
title: Testing
description: Pruebas unitarias de reintentos, circuit breaker y utilidades
---

## Ejemplos con Bun

```ts
import { describe, it, expect } from "bun:test";
import { run, createRunner } from "runtry";
import { computeBackoffDelay } from "runtry/dist/utils";

describe("run: success", () => {
  it("retorna ok con data", async () => {
    const r = await run(async () => 42);
    expect(r.ok).toBe(true);
  });
});

describe("utils: backoff", () => {
  it("linear/exponential/fibonacci", () => {
    expect(computeBackoffDelay("linear", 100, 3)).toBe(100);
  });
});
```
